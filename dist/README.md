# Distribution Files

This directory contains the compiled/bundled version of the card.

After running `npm run build`, the compiled `elbot-card.js` will be placed here.

For development, you can run `npm run watch` to automatically rebuild when source files change.
